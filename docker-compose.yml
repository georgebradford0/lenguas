name: lenguas-dev

services:
  api:
    build: ./api/v0
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
      - AWS_REGION=${AWS_REGION:-us-east-1}

volumes:
  mongo-data:
    driver: local

networks:
  language-app-network:
    driver: bridge
